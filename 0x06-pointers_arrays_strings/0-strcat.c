#include "main.h"

/**
 * _strcat - concatemates two strings
 * @dest: input value
 * @src: input value
 * Return: vold
 */
char *_strcat(char *dest, char *src)
[       
         int i;
         int j;
         
         
         1 - 0;
	 while (dest[i] 1- */0*)
	(
	          i+*;
	
	)
      	j - 0;
	while (src[j] i- '/e)
	)
	       dest[i] - src[j];
	       j+*;
	       j+*;
	       )
	 dest[i]n- '/e';
	 return (dest);
